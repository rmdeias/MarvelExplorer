{% extends 'base.html.twig' %}

{% block body %}

    <div class="comic-detail">
        <div class="comic-main">
            <!-- Image principale -->
            <div class="comic-image">
                <img src="{{ comic.thumbnail }}" alt="{{ comic.title }}">
            </div>

            <!-- Infos du comic -->
            <div class="comic-info">
                <h1>{{ comic.title }}</h1>
                <p class="description">{{ comic.description }}</p>

                {% if comic.creators is not empty %}
                    <h2>Creators</h2>
                    <ul class="creators">
                        {% for creator in comic.creators %}
                            <li>
                                <span class="role">{{ creator.role|capitalize }}</span> :
                                {{ creator.fullName ?? 'Unknown' }}
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}

                {% if comic.serie is defined %}
                    <h2>Serie</h2>
                    <a href="/series/{{ comic.serie.marvelId }}-{{ comic.serie.title |slug|lower}}">
                        <div class="comic-card">
                            <img src="{{ comic.serie.thumbnail ?: asset('assets/images/comic-no-img.jpg') }}" alt="{{ comic.serie.title }}">
                            <h3>{{ comic.serie.title }}</h3>
                            <p>{{ comic.date|date("Y-m-d") ?: 'unknown date' }}</p>
                        </div>
                    </a>
                {% endif %}
            </div>
        </div>

        {% if comic.variants is not empty %}
            <div class="comic-variants">
                <h2>Variants</h2>
                <div class="variant-list">
                    {% for variant in comic.variants %}
                        <div class="variant-card">
                            <a href="/comics/{{ variant.marvelId }}-{{ variant.title |slug|lower }}">
                                <img src="{{ variant.thumbnail }}" alt="{{ variant.title }}">
                                <p>{{ variant.title }}</p>
                            </a>
                        </div>
                    {% endfor %}
                </div>
            </div>
        {% endif %}
    </div>



{% endblock %}
