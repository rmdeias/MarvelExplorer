<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Comics</title>
    <style>
        body { font-family: Arial; margin: 2rem; background: #f5f5f5; }
        h1 { color: #222; }
        #search-bar { margin-bottom: 1rem; padding: 0.5rem; width: 300px; }
        .comic-list { display: flex; flex-wrap: wrap; gap: 1rem; }
        .comic-card { background: #fff; border-radius: 8px; box-shadow: 0 2px 6px rgba(0,0,0,0.15); width: 180px; text-align: center; }
        .comic-card img { width: 100%; height: 250px; object-fit: cover; border-bottom: 1px solid #ddd; }
        .comic-card h3 { font-size: 1rem; margin: 0.5rem 0 0.25rem 0; }
        .comic-card p { font-size: 0.8rem; color: #555; }
    </style>
</head>
<body>
        {% block body %}{% endblock %}
</body>
</html>
